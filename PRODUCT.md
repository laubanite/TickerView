# Product

## Register

product

## Users

A 股中长线 ETF 交易者(上班族):盘中注意力稀缺、下单在手机,
依赖"作战地图(周级计划)+ 实时行情"的核对工作流。
定位 = 上班族的盯盘工具:不写死资金规模与持仓数量(典型案例是小资金账户,但任何规模适用);
ETF 是当前可落地形态,个股支持为预留扩展点(见 TODO)。
情绪特征(常见散户共性):止损触发时不愿全清(倾向留底仓等结构确认的加仓点),需要护栏防退化。

## Product Purpose

个人交易的"执行助手":把作战地图(计划)与实时行情核成一眼能懂的状态,
替代"上班偷偷盯盘 + 大脑心算"。不是行情软件、不是自动交易——所有下单由人执行,
系统只出判断、提示与建议(手动下单,自动提醒)。

核心契约:**规则引擎算确定性信号,LLM 只做解读;输出必须肉眼可复核,拒绝黑箱。**
数据快照(程序计算)与深入分析(LLM)两张卡共用同一份 markdown——"用户看到的 = 模型看到的",
幻觉可被肉眼复核。成功 = 用户每天多花不到 1 分钟就能决定"今天动还是不动"。

## Brand Personality

克制、严谨、可复核。像可信的数据终端,不像营销页:结论词打头、数字精确到小数点、
来源标注明确(腾讯/新浪/Wind/东财),状态标签由程序标注而非 LLM 自拟。

## Anti-references

- 黑箱 AI 建议(无法对账的"我觉得会涨")——必须能溯源到快照中的数字或作战地图规则
- 机构风格的浮夸措辞(如"强势突破在即")——数据不支持就闭嘴
- 数据堆砌不标结论(需要用户换算的页面)——面板只显示结论
- 持仓"开超市"(同时持有很多只)——设计上有持仓上限护栏

## Design Principles

1. **面板只显示结论**,不展示需要换算的数据(量比→放量/缩量,距触发百分比→接近买点/等待)
2. **差异输出**:有动作的重点写,无变化的一行带过或置暗
3. **分层各管一件事**:速查表管基准、剧本管变化、快照管实时、提醒管兜底
4. **计划层不被机器污染**:AI 生成内容人工确认后才写入作战地图
5. **数字可复核**:确定性数值由程序计算并标注状态(超买/超卖/站上/受制),LLM 只准沿用
6. **手动下单,自动提醒**:绝不自动交易,但主动推送触发状态

## Accessibility & Inclusion

深色交易主题(style.css 令牌:--bg #0b0e14 / --text #d1d4dc / --dim #787b86),
A 股红涨绿跌(up = --red,down = --green)。正文对比度满足 ≥4.5:1,弱化文字用 --dim
而非降低透明度。无已记录的特殊辅助需求;动效以轻量过渡为主,不阻塞内容可见性。